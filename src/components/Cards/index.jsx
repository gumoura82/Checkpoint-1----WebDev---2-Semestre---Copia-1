export default function Cards({props}){
    return(
        <>
        {
            props.map((dados, index) => (
                <div className="card-social">

                </div>



            ))
        }
        </>
    )
}